export { default as apiClient } from './api/apiClient';
export { default as ClientProvider } from './providers/ClientProvider';

// Make sure the client is properly exported
export const client = {
  // Your client implementation
};

