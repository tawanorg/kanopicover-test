version: '3'

services:

  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5001:80"
    tty: true
    volumes:
      - ./backend:/app
    networks:
      - kanopi-network
     
  frontend:
    build: ./frontend
    tty: true
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    depends_on:
      - backend
    networks:
      - kanopi-network

networks:
  kanopi-network:
    driver: bridge